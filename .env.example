# Copy this file to `.env` (do NOT commit `.env`) and fill in your Supabase project values.
# For Vite, use the VITE_ prefix so values are exposed to client code.

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-public-anon-key

# Optional: Server-only service role key (keep secret, do NOT expose to the browser)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Example usage:
# 1. cp .env.example .env
# 2. Fill values
# 3. cd joustry-fresh
# 4. pnpm install && pnpm run dev
