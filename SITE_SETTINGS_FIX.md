# إصلاح صفحة إعدادات الموقع ✅

## المشكلة
كانت صفحة إعدادات الموقع على Netlify تعرض رسالة "قريباً..." بدلاً من عرض التابات الكاملة.

## السبب
الملف `src/components/dashboard/SiteSettings.tsx` كان يحتوي على كود بسيط يعرض رسالة "قريباً" فقط، بينما الكود الكامل موجود في ملف آخر اسمه `SiteSettingsManager.tsx`.

## الحل
تم تعديل ملف `SiteSettings.tsx` ليستورد ويستخدم `SiteSettingsManager` مباشرة.

### الملفات المعدلة:
- ✅ `src/components/dashboard/SiteSettings.tsx` - تم إصلاحه

### الملفات المستخدمة:
- ✅ `src/components/dashboard/SiteSettingsManager.tsx` - يحتوي على كل التابات
- ✅ `src/hooks/useTheme.ts` - لإدارة الألوان
- ✅ `src/components/ui/ImageUploader.tsx` - لرفع الصور
- ✅ `src/lib/dataService.ts` - للتواصل مع Supabase

## التابات المتوفرة الآن:
1. ✅ **إعدادات عامة** - اسم الموقع، الوصف، اللوجو، الألوان
2. ✅ **معلومات التواصل** - الهاتف، البريد، العنوان، ساعات العمل
3. ✅ **وسائل التواصل** - Facebook, Twitter, Instagram, YouTube
4. ✅ **تحسين محركات البحث (SEO)** - Meta Title, Description, Keywords
5. ✅ **إعدادات متقدمة** - Google Analytics

## الخطوات التالية:
1. قم برفع التعديلات على Git:
   ```bash
   git add .
   git commit -m "Fix: Site Settings page now shows all tabs"
   git push
   ```

2. Netlify سيقوم بإعادة البناء تلقائياً

3. بعد اكتمال البناء، افتح الموقع وادخل على:
   - الداشبورد: `https://your-site.netlify.app/admin/dashboard`
   - اضغط على "إعدادات الموقع"
   - ستظهر جميع التابات الآن! 🎉

## ملاحظات مهمة:
- ✅ Supabase متصل ويعمل بشكل صحيح
- ✅ جدول `site_settings` موجود في قاعدة البيانات
- ✅ جميع الملفات المطلوبة موجودة
- ✅ الكود جاهز للعمل على Netlify

## اختبار محلي (اختياري):
```bash
npm run dev
```
ثم افتح: `http://localhost:5173/admin/dashboard`
