# 🔒 دليل Row Level Security (RLS)

## 📖 ما هو RLS؟

**Row Level Security (RLS)** هو نظام حماية في Supabase يتحكم في من يمكنه:
- 📖 قراءة البيانات
- ➕ إضافة بيانات
- ✏️ تعديل البيانات
- 🗑️ حذف البيانات

---

## 🔓 تعطيل RLS لجميع الجداول

### **شغل في Supabase SQL Editor:**
```sql
-- انسخ الكود من:
database/DISABLE_RLS_ALL.sql
```

### **النتيجة:**
- ✅ جميع الجداول ستكون **مفتوحة**
- ✅ أي شخص يمكنه القراءة/الكتابة/التعديل/الحذف
- ✅ **لا توجد قيود**

### **متى تستخدم هذا؟**
- ✅ في مرحلة التطوير
- ✅ إذا كنت تريد وصول كامل بدون قيود
- ✅ إذا كانت الـ Policies تسبب مشاكل

---

## 🔒 تفعيل RLS لجميع الجداول

### **شغل في Supabase SQL Editor:**
```sql
-- انسخ الكود من:
database/ENABLE_RLS_ALL.sql
```

### **النتيجة:**
- 🔒 جميع الجداول ستكون **محمية**
- 🔒 يجب وجود **Policies** للسماح بالوصول
- 🔒 بدون Policies = **لا يمكن الوصول للبيانات**

### **متى تستخدم هذا؟**
- ✅ في مرحلة الإنتاج
- ✅ إذا كنت تريد حماية البيانات
- ✅ إذا كنت تريد التحكم في من يمكنه الوصول

---

## 🎯 الطريقة الموصى بها:

### **للتطوير (الآن):**
```sql
-- عطل RLS
database/DISABLE_RLS_ALL.sql
```
✅ **سهل وسريع، لا توجد مشاكل!**

### **للإنتاج (لاحقاً):**
```sql
-- فعل RLS
database/ENABLE_RLS_ALL.sql

-- ثم أضف Policies
database/FIX_POLICIES_CLEAN.sql
```
🔒 **آمن ومحمي!**

---

## 📊 الفرق:

| الميزة | RLS معطل | RLS مفعل |
|--------|---------|----------|
| الوصول | 🔓 مفتوح للجميع | 🔒 محمي بـ Policies |
| السهولة | ✅ سهل جداً | ⚠️ يحتاج Policies |
| الأمان | ❌ غير آمن | ✅ آمن |
| التطوير | ✅ مثالي | ⚠️ قد يسبب مشاكل |
| الإنتاج | ❌ غير موصى به | ✅ موصى به |

---

## 🔧 كيفية التعطيل من Supabase Dashboard:

### **الطريقة اليدوية:**

1. **اذهب لـ:**
   ```
   Supabase Dashboard
   → Authentication
   → Policies
   ```

2. **اختر الجدول** (مثل `products`)

3. **اضغط "Disable RLS"**

4. **كرر** لكل جدول

### **الطريقة السريعة (SQL):**
```sql
-- شغل ملف واحد فقط:
database/DISABLE_RLS_ALL.sql
```
✅ **يعطل RLS لجميع الجداول دفعة واحدة!**

---

## ⚠️ تحذيرات مهمة:

### **عند تعطيل RLS:**
- ⚠️ أي شخص عنده الـ `anon key` يمكنه الوصول لكل البيانات
- ⚠️ لا توجد حماية للبيانات الحساسة
- ⚠️ **لا تستخدم في الإنتاج!**

### **عند تفعيل RLS:**
- ⚠️ يجب وجود **Policies** صحيحة
- ⚠️ بدون Policies = **لا يمكن الوصول للبيانات**
- ⚠️ قد تحتاج تعديل الـ Policies حسب احتياجك

---

## 🎯 التوصية الحالية:

**للتطوير السريع:**
```sql
-- عطل RLS لجميع الجداول
database/DISABLE_RLS_ALL.sql
```

**النتيجة:**
- ✅ لا توجد مشاكل في الحفظ
- ✅ لا توجد أخطاء RLS
- ✅ كل شيء يعمل بسلاسة
- ✅ يمكنك التركيز على التطوير

**لاحقاً (قبل الإطلاق):**
```sql
-- فعل RLS وأضف Policies
database/ENABLE_RLS_ALL.sql
database/FIX_POLICIES_CLEAN.sql
```

---

## 📝 ملاحظات:

1. **الملفات المتوفرة:**
   - `DISABLE_RLS_ALL.sql` - تعطيل RLS لجميع الجداول
   - `ENABLE_RLS_ALL.sql` - تفعيل RLS لجميع الجداول
   - `FIX_POLICIES_CLEAN.sql` - إضافة Policies صحيحة

2. **الاستخدام:**
   - شغل ملف واحد فقط في Supabase SQL Editor
   - النتيجة فورية
   - يمكنك التبديل بين التفعيل والتعطيل بسهولة

3. **التحقق:**
   - بعد التشغيل، سيعرض حالة RLS لكل جدول
   - 🔓 معطل = مفتوح للجميع
   - 🔒 مفعل = محمي بـ Policies

---

## 🚀 ابدأ الآن:

**شغل في Supabase SQL Editor:**
```sql
database/DISABLE_RLS_ALL.sql
```

**النتيجة:**
- ✅ RLS معطل لجميع الجداول
- ✅ لا توجد مشاكل في الحفظ
- ✅ كل شيء يعمل!

**جرب الآن!** 🎉
