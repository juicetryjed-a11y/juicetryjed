# 🚀 **دليل إعداد Backend على Supabase**

## 📋 **نظرة عامة:**

تم بناء Backend متكامل على Supabase يتضمن:
- ✅ **9 جداول رئيسية** مع علاقات منطقية
- ✅ **Row Level Security (RLS)** للأمان
- ✅ **Triggers تلقائية** للعمليات
- ✅ **Functions مساعدة** للإحصائيات
- ✅ **بيانات تجريبية** للبدء السريع

---

## 🏗️ **هيكل قاعدة البيانات:**

### **الجداول الرئيسية:**

```
1. categories          → التصنيفات (عصائر، سموثي، إلخ)
2. products            → المنتجات مع جميع التفاصيل
3. orders              → الطلبات
4. order_items         → عناصر كل طلب
5. customer_reviews    → مراجعات العملاء
6. blog_posts          → مقالات المدونة
7. profiles            → بيانات المستخدمين
8. site_settings       → إعدادات الموقع
9. about_page_settings → إعدادات صفحة من نحن
```

---

## 📝 **خطوات الإعداد:**

### **الخطوة 1: إنشاء مشروع Supabase**

```bash
1. اذهب إلى: https://supabase.com
2. سجل دخول أو أنشئ حساب جديد
3. انقر "New Project"
4. املأ البيانات:
   - Project Name: juicetry
   - Database Password: (احفظه في مكان آمن)
   - Region: اختر الأقرب لك
5. انقر "Create new project"
6. انتظر 2-3 دقائق حتى يكتمل الإعداد
```

---

### **الخطوة 2: الحصول على مفاتيح API**

```bash
1. في لوحة تحكم Supabase
2. اذهب إلى: Settings > API
3. انسخ:
   ✅ Project URL
   ✅ anon public key
```

**مثال:**
```
URL: https://odkdecaasvvgpgatated.supabase.co
Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

### **الخطوة 3: تحديث ملف .env**

افتح ملف `.env` في المشروع وحدث القيم:

```env
# Supabase Configuration
VITE_SUPABASE_URL=https://YOUR_PROJECT_ID.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE

# استخدام قاعدة البيانات الحقيقية
VITE_USE_MOCK_DATA=false
```

**⚠️ مهم:** استبدل `YOUR_PROJECT_ID` و `YOUR_ANON_KEY_HERE` بالقيم الفعلية!

---

### **الخطوة 4: تنفيذ SQL Script**

#### **الطريقة الأولى: SQL Editor (موصى بها)**

```bash
1. في لوحة تحكم Supabase
2. اذهب إلى: SQL Editor
3. انقر "New Query"
4. افتح ملف: database/complete_backend_setup.sql
5. انسخ كل المحتوى والصقه في SQL Editor
6. انقر "Run" أو اضغط Ctrl+Enter
7. انتظر حتى يكتمل التنفيذ (قد يستغرق 10-30 ثانية)
```

#### **الطريقة الثانية: من خلال CLI**

```bash
# إذا كنت تستخدم Supabase CLI
supabase db reset
supabase db push
```

---

### **الخطوة 5: إنشاء مستخدم إداري**

#### **5.1 إنشاء المستخدم:**

```bash
1. في Supabase Dashboard
2. اذهب إلى: Authentication > Users
3. انقر "Add user" > "Create new user"
4. املأ البيانات:
   Email: admin@juicetry.com
   Password: admin123
   Auto Confirm User: ✅ نعم
5. انقر "Create user"
6. انسخ User UUID (سنحتاجه في الخطوة التالية)
```

#### **5.2 تحديث صلاحيات المستخدم:**

```sql
-- في SQL Editor، نفذ هذا الأمر:
-- استبدل 'USER_UUID_HERE' بـ UUID المستخدم الذي نسخته

INSERT INTO profiles (id, email, full_name, role, is_active)
VALUES (
  'USER_UUID_HERE',
  'admin@juicetry.com',
  'Admin User',
  'admin',
  true
);
```

**مثال:**
```sql
INSERT INTO profiles (id, email, full_name, role, is_active)
VALUES (
  'a1b2c3d4-e5f6-7890-abcd-ef1234567890',
  'admin@juicetry.com',
  'Admin User',
  'admin',
  true
);
```

---

### **الخطوة 6: التحقق من الإعداد**

#### **6.1 فحص الجداول:**

```sql
-- في SQL Editor
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;
```

**يجب أن ترى:**
```
✅ about_page_settings
✅ blog_posts
✅ categories
✅ customer_reviews
✅ order_items
✅ orders
✅ products
✅ profiles
✅ site_settings
```

#### **6.2 فحص البيانات التجريبية:**

```sql
-- فحص التصنيفات
SELECT id, name, name_en, is_active FROM categories;

-- فحص المنتجات
SELECT id, name, price, category_id, is_active FROM products;

-- فحص الإعدادات
SELECT key, value FROM site_settings;
```

#### **6.3 فحص RLS:**

```sql
-- فحص السياسات
SELECT tablename, policyname, permissive, roles, cmd
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

---

## 🔐 **نظام الأمان (RLS):**

### **السياسات المطبقة:**

#### **1. القراءة العامة (Public Read):**
```
✅ categories          → النشطة فقط (is_active = true)
✅ products            → النشطة فقط (is_active = true)
✅ customer_reviews    → الموافق عليها فقط (is_approved = true)
✅ blog_posts          → المنشورة فقط (is_published = true)
✅ site_settings       → الكل
✅ about_page_settings → النشطة فقط
```

#### **2. صلاحيات الإدارة (Admin):**
```
✅ جميع الجداول → قراءة، إضافة، تعديل، حذف
✅ التحقق من الصلاحية عبر: is_admin() function
✅ يجب أن يكون role = 'admin' أو 'manager'
```

#### **3. صلاحيات العملاء (Customers):**
```
✅ إنشاء طلبات جديدة
✅ إضافة مراجعات
✅ قراءة البيانات العامة
```

---

## ⚙️ **الـ Triggers التلقائية:**

### **1. تحديث updated_at:**
```sql
-- يتم تحديث updated_at تلقائياً عند أي تعديل
-- على جميع الجداول
```

### **2. تحديث تقييمات المنتجات:**
```sql
-- عند إضافة/تعديل/حذف مراجعة:
-- ✅ يتم حساب متوسط التقييم تلقائياً
-- ✅ يتم تحديث عدد المراجعات
```

### **3. إنشاء رقم طلب تلقائي:**
```sql
-- عند إنشاء طلب جديد:
-- ✅ يتم إنشاء order_number بصيغة: ORD-20241107-000001
```

### **4. إنشاء slug للمنتجات:**
```sql
-- عند إضافة منتج جديد:
-- ✅ يتم إنشاء slug تلقائياً من اسم المنتج
```

---

## 🔧 **الدوال المساعدة (Functions):**

### **1. حساب إجمالي الطلب:**
```sql
SELECT calculate_order_total(order_id);
-- يحسب مجموع أسعار جميع عناصر الطلب
```

### **2. المنتجات الأكثر مبيعاً:**
```sql
SELECT * FROM get_bestselling_products(10);
-- يعرض أفضل 10 منتجات مبيعاً
```

### **3. إحصائيات الطلبات:**
```sql
SELECT * FROM get_orders_statistics(
  '2024-01-01'::timestamptz,
  '2024-12-31'::timestamptz
);
-- يعرض إحصائيات الطلبات في فترة محددة
```

---

## 📊 **هيكل الجداول بالتفصيل:**

### **1. جدول categories:**
```sql
- id                 → معرف فريد
- name               → الاسم بالعربية
- name_en            → الاسم بالإنجليزية
- description        → الوصف
- icon               → أيقونة (emoji)
- color              → اللون (#hex)
- image_url          → رابط الصورة
- is_active          → نشط/غير نشط
- order_index        → ترتيب العرض
- created_at         → تاريخ الإنشاء
- updated_at         → تاريخ آخر تحديث
```

### **2. جدول products:**
```sql
- id                    → معرف فريد
- name                  → الاسم بالعربية
- name_en               → الاسم بالإنجليزية
- description           → الوصف بالعربية
- description_en        → الوصف بالإنجليزية
- price                 → السعر
- original_price        → السعر الأصلي (قبل الخصم)
- category_id           → معرف التصنيف
- image_url             → الصورة الرئيسية
- images                → صور إضافية (JSONB array)
- ingredients           → المكونات (array)
- nutritional_info      → معلومات غذائية (JSONB)
- allergens             → مسببات الحساسية (array)
- sizes                 → الأحجام المتاحة (JSONB)
- available_sizes       → أحجام متاحة (array)
- customization_options → خيارات التخصيص (JSONB)
- is_active             → نشط/غير نشط
- is_featured           → منتج مميز
- is_new                → منتج جديد
- is_bestseller         → الأكثر مبيعاً
- in_stock              → متوفر في المخزون
- stock_quantity        → كمية المخزون
- rating                → التقييم (0-5)
- reviews_count         → عدد المراجعات
- slug                  → رابط صديق لمحركات البحث
- meta_title            → عنوان SEO
- meta_description      → وصف SEO
- created_at            → تاريخ الإنشاء
- updated_at            → تاريخ آخر تحديث
```

### **3. جدول orders:**
```sql
- id                  → معرف فريد
- order_number        → رقم الطلب (تلقائي)
- customer_name       → اسم العميل
- customer_email      → بريد العميل
- customer_phone      → هاتف العميل
- customer_address    → عنوان العميل
- customer_notes      → ملاحظات العميل
- total_amount        → المجموع الأولي
- discount_amount     → قيمة الخصم
- delivery_fee        → رسوم التوصيل
- final_amount        → المجموع النهائي
- status              → حالة الطلب
- payment_status      → حالة الدفع
- payment_method      → طريقة الدفع
- delivery_type       → نوع التوصيل
- delivery_address    → عنوان التوصيل
- delivery_time       → وقت التوصيل
- estimated_delivery  → الوقت المتوقع
- created_at          → تاريخ الإنشاء
- updated_at          → تاريخ آخر تحديث
- confirmed_at        → تاريخ التأكيد
- delivered_at        → تاريخ التسليم
```

### **4. جدول order_items:**
```sql
- id              → معرف فريد
- order_id        → معرف الطلب
- product_id      → معرف المنتج
- product_name    → اسم المنتج (وقت الطلب)
- product_image   → صورة المنتج
- quantity        → الكمية
- unit_price      → سعر الوحدة
- total_price     → السعر الإجمالي
- size            → الحجم المختار
- customizations  → التخصيصات (JSONB)
- notes           → ملاحظات
- created_at      → تاريخ الإنشاء
```

### **5. جدول customer_reviews:**
```sql
- id              → معرف فريد
- product_id      → معرف المنتج
- order_id        → معرف الطلب (اختياري)
- customer_name   → اسم العميل
- customer_email  → بريد العميل
- rating          → التقييم (1-5)
- title           → عنوان المراجعة
- comment         → نص المراجعة
- images          → صور المراجعة (array)
- is_verified     → مراجعة موثقة
- is_approved     → موافق عليها
- is_featured     → مراجعة مميزة
- admin_reply     → رد الإدارة
- admin_reply_at  → تاريخ الرد
- created_at      → تاريخ الإنشاء
- updated_at      → تاريخ آخر تحديث
```

---

## 🧪 **اختبار Backend:**

### **اختبار 1: إضافة منتج**

```sql
INSERT INTO products (
  name, 
  name_en, 
  description, 
  price, 
  category_id, 
  image_url,
  is_active
) VALUES (
  'عصير تفاح',
  'Apple Juice',
  'عصير تفاح طبيعي 100%',
  12.00,
  1,
  'https://images.unsplash.com/photo-1560806887-1e4cd0b6cbd6?w=400',
  true
);

-- التحقق
SELECT * FROM products WHERE name = 'عصير تفاح';
```

### **اختبار 2: إنشاء طلب**

```sql
-- إنشاء الطلب
INSERT INTO orders (
  customer_name,
  customer_phone,
  customer_address,
  total_amount,
  final_amount,
  status
) VALUES (
  'أحمد محمد',
  '+966501234567',
  'الرياض، حي النخيل',
  50.00,
  60.00,
  'pending'
) RETURNING id;

-- افترض أن id = 1
-- إضافة عناصر الطلب
INSERT INTO order_items (
  order_id,
  product_id,
  product_name,
  quantity,
  unit_price,
  total_price
) VALUES (
  1,
  1,
  'عصير برتقال طازج',
  2,
  15.00,
  30.00
);

-- التحقق
SELECT * FROM orders WHERE id = 1;
SELECT * FROM order_items WHERE order_id = 1;
```

### **اختبار 3: إضافة مراجعة**

```sql
INSERT INTO customer_reviews (
  product_id,
  customer_name,
  customer_email,
  rating,
  title,
  comment,
  is_approved
) VALUES (
  1,
  'سارة أحمد',
  'sara@example.com',
  5,
  'ممتاز جداً',
  'عصير طازج ولذيذ، أنصح به بشدة!',
  true
);

-- التحقق من تحديث تقييم المنتج تلقائياً
SELECT id, name, rating, reviews_count 
FROM products 
WHERE id = 1;
```

---

## 🔍 **استعلامات مفيدة:**

### **عرض جميع المنتجات مع تصنيفاتها:**
```sql
SELECT 
  p.id,
  p.name,
  p.price,
  c.name as category_name,
  p.rating,
  p.reviews_count,
  p.is_active
FROM products p
LEFT JOIN categories c ON p.category_id = c.id
ORDER BY p.created_at DESC;
```

### **عرض الطلبات مع عدد العناصر:**
```sql
SELECT 
  o.id,
  o.order_number,
  o.customer_name,
  o.final_amount,
  o.status,
  COUNT(oi.id) as items_count,
  o.created_at
FROM orders o
LEFT JOIN order_items oi ON o.id = oi.order_id
GROUP BY o.id
ORDER BY o.created_at DESC;
```

### **عرض المنتجات الأكثر تقييماً:**
```sql
SELECT 
  id,
  name,
  rating,
  reviews_count,
  price
FROM products
WHERE is_active = true
  AND reviews_count > 0
ORDER BY rating DESC, reviews_count DESC
LIMIT 10;
```

---

## 🚀 **تشغيل المشروع:**

### **1. تثبيت المكتبات:**
```bash
npm install
```

### **2. تشغيل الخادم:**
```bash
npm run dev
```

### **3. فتح المتصفح:**
```
http://localhost:5174
```

### **4. تسجيل الدخول للإدارة:**
```
URL: http://localhost:5174/admin/login
Email: admin@juicetry.com
Password: admin123
```

---

## ✅ **التحقق من عمل Backend:**

### **اختبار شامل:**

```bash
1. افتح لوحة التحكم
2. اذهب لتاب "المنتجات"
3. أضف منتج جديد
4. احفظ المنتج
5. افتح صفحة المنيو في تاب آخر
6. تأكد من ظهور المنتج الجديد
```

**النتيجة المتوقعة:**
```
✅ المنتج يُضاف لقاعدة البيانات Supabase
✅ المنتج يظهر فوراً في لوحة التحكم
✅ المنتج يظهر فوراً في صفحة المنيو
✅ الصورة تظهر بشكل صحيح
✅ جميع البيانات محفوظة
```

---

## 🎯 **الخلاصة:**

### **ما تم بناؤه:**

✅ **قاعدة بيانات متكاملة** مع 9 جداول رئيسية
✅ **نظام أمان RLS** محكم ومتقدم
✅ **Triggers تلقائية** للعمليات الشائعة
✅ **Functions مساعدة** للإحصائيات والتقارير
✅ **بيانات تجريبية** للبدء السريع
✅ **علاقات منطقية** بين الجداول
✅ **فهارس محسنة** للأداء السريع

### **المميزات:**

✅ **منطق برمجي سليم** ومتقدم
✅ **أمان عالي** مع RLS
✅ **أداء ممتاز** مع الفهارس
✅ **سهولة الصيانة** والتطوير
✅ **قابلية التوسع** في المستقبل

---

## 📞 **الدعم:**

إذا واجهت أي مشكلة:

1. تحقق من ملف `.env` وتأكد من صحة المفاتيح
2. تحقق من تنفيذ SQL Script بنجاح
3. تحقق من إنشاء المستخدم الإداري
4. راجع Console في المتصفح للأخطاء
5. راجع Logs في Supabase Dashboard

**🎉 Backend جاهز للاستخدام بشكل كامل!**
