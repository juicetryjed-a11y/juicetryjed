# 🚀 دليل تحديث الموقع على Netlify

## الطريقة 1: رفع يدوي (الأسهل)

### الخطوة 1: بناء المشروع
```bash
npm run build
```

### الخطوة 2: رفع على Netlify
1. اذهب إلى [Netlify Dashboard](https://app.netlify.com/)
2. اختر موقعك (Juicetry)
3. اسحب مجلد `dist` إلى منطقة "Drag and drop"
4. انتظر حتى ينتهي الرفع

---

## الطريقة 2: باستخدام Netlify CLI

### تثبيت Netlify CLI:
```bash
npm install -g netlify-cli
```

### تسجيل الدخول:
```bash
netlify login
```

### بناء ورفع:
```bash
npm run build
netlify deploy --prod
```

---

## الطريقة 3: ربط مع GitHub (الأفضل للمستقبل)

### إذا أردت ربط المشروع بـ GitHub:

1. **إنشاء repository على GitHub:**
   - اذهب إلى [GitHub](https://github.com)
   - اضغط "New repository"
   - سمّه `joustry-fresh`

2. **رفع الكود:**
   ```bash
   git init
   git add .
   git commit -m "Update dashboard with new features"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/joustry-fresh.git
   git push -u origin main
   ```

3. **ربط Netlify بـ GitHub:**
   - في Netlify Dashboard
   - Site settings > Build & deploy
   - اختر "Link to GitHub"
   - اختر repository
   - كل push سيحدث الموقع تلقائياً!

---

## ✅ التحديثات الجديدة في هذه النسخة:

### 1. إدارة المستخدمين
- ✅ إضافة/تعديل/حذف مستخدمين
- ✅ تغيير الأدوار (Admin/Customer)
- ✅ بحث وفلترة

### 2. إدارة الطلبات
- ✅ عرض جميع الطلبات
- ✅ تحديث حالة الطلب
- ✅ تحديث حالة الدفع
- ✅ عرض تفاصيل كل طلب

### 3. إدارة المراجعات
- ✅ قبول/رفض المراجعات
- ✅ نظام النجوم
- ✅ فلترة حسب الحالة

### 4. إدارة العملاء
- ✅ قاعدة بيانات كاملة للعملاء
- ✅ تتبع الطلبات والمبيعات

### 5. إدارة المقالات
- ✅ نشر/إخفاء المقالات
- ✅ نظام الفئات والوسوم

---

## 🔧 ملاحظات مهمة:

### تحديث متغيرات البيئة في Netlify:
1. اذهب إلى Site settings > Environment variables
2. تأكد من:
   ```
   VITE_SUPABASE_URL = https://ijpugtvfckmptzegdchr.supabase.co
   VITE_SUPABASE_ANON_KEY = [موجود في netlify.toml]
   VITE_USE_MOCK_DATA = false
   VITE_DISABLE_LOCALSTORAGE = true
   ```

### تحديث قاعدة البيانات:
قبل نشر الموقع، تأكد من تشغيل:
1. `database/fresh_dashboard_tables.sql` - إنشاء الجداول
2. `database/insert_sample_data.sql` - إضافة بيانات تجريبية

---

## 🎯 بعد الرفع:

1. ✅ تحقق من الموقع: `https://your-site.netlify.app`
2. ✅ اختبر لوحة التحكم
3. ✅ تأكد من عمل جميع الصفحات
4. ✅ تحقق من Console (F12) للتأكد من عدم وجود أخطاء

---

## 📞 إذا واجهت مشاكل:

### خطأ في البناء:
```bash
# امسح node_modules وأعد التثبيت
rm -rf node_modules
npm install
npm run build
```

### خطأ في الاتصال بـ Supabase:
- تحقق من Environment Variables في Netlify
- تأكد أن الجداول موجودة في Supabase
- تحقق من RLS Policies

**الموقع جاهز للتحديث!** 🚀
