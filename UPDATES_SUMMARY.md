# ملخص التحديثات المطلوبة

## ✅ ما تم إنجازه

### 1. إضافة روابط وسائل التواصل الاجتماعي
- ✅ تم إنشاء ملف SQL لإضافة Snapchat و Facebook: `database/add_social_links.sql`
- ✅ تم تحديث `useFooterSettings.ts` لإضافة `snapchat_url`
- ✅ تم تحديث `Footer.tsx` لعرض أيقونات Snapchat و Facebook الرسمية
- ✅ تم تحديث `SiteSettingsManager.tsx` لإضافة حقل Snapchat في لوحة التحكم

### 2. إصلاح عرض صور المقالات
- ✅ تم تحديث `FastBlogPage.tsx` لعرض `image_url` بدلاً من emoji فقط

## 📋 الخطوات المطلوبة منك

### الخطوة 1: تحديث قاعدة البيانات
1. افتح Supabase SQL Editor
2. شغّل الكود من ملف: `database/add_social_links.sql`
3. هذا سيضيف:
   - عمود `snapchat_url` و `facebook_url` للجداول
   - الروابط التالية:
     - Snapchat: `https://www.snapchat.com/add/juicetryjed`
     - Facebook: `https://www.facebook.com/share/14Mjo53Fn8V/?mibextid=wwXIfr`

### الخطوة 2: إعادة تشغيل الخادم
```bash
# أوقف الخادم الحالي (Ctrl+C)
# ثم شغّله من جديد
npm run dev
```

### الخطوة 3: التحقق من النتائج
1. افتح الموقع: `http://localhost:5173`
2. تحقق من الفوتر - يجب أن ترى أيقونات:
   - Facebook
   - Instagram
   - Twitter
   - TikTok
   - Snapchat (جديد)
3. افتح صفحة المدونة `/blog` - يجب أن ترى صور المقالات الحقيقية

### الخطوة 4: إدارة الروابط من لوحة التحكم
1. اذهب إلى: `/admin/dashboard`
2. اختر **إعدادات الموقع**
3. اختر تبويب **وسائل التواصل**
4. يمكنك تعديل جميع الروابط من هنا

## 🔧 في حالة وجود مشاكل

### إذا لم تظهر الأيقونات:
1. تأكد من تشغيل كود SQL في Supabase
2. أعد تشغيل الخادم
3. امسح الكاش: Ctrl+Shift+R

### إذا لم تظهر صور المقالات:
- تأكد من أن المقالات في قاعدة البيانات تحتوي على `image_url`
- يمكنك إضافة الصور من لوحة التحكم > إدارة المقالات

## 📁 الملفات المعدلة
- `database/add_social_links.sql` - كود SQL لإضافة الأعمدة
- `src/hooks/useFooterSettings.ts` - إضافة snapchat_url
- `src/components/layout/Footer.tsx` - عرض أيقونات Snapchat و Facebook
- `src/components/dashboard/SiteSettingsManager.tsx` - حقل Snapchat في الداشبورد
- `src/pages/FastBlogPage.tsx` - عرض صور المقالات

## 🎯 النتيجة النهائية
- ✅ روابط Snapchat و Facebook في الفوتر
- ✅ أيقونات رسمية لجميع وسائل التواصل
- ✅ أسماء المنصات تحت كل أيقونة
- ✅ صور المقالات تظهر بشكل صحيح
- ✅ إمكانية إدارة كل شيء من لوحة التحكم
