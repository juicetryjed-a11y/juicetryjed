# 🚨 إصلاح خطأ 400 Bad Request

## المشكلة
```
POST 400 (Bad Request)
```

## السبب
الجداول غير موجودة أو فارغة في قاعدة البيانات.

## ✅ الحل السريع - 3 خطوات

### الخطوة 1: تحقق من الجداول
شغل في Supabase SQL Editor:
```sql
-- انسخ من: database/check_tables.sql
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('orders', 'order_items', 'reviews', 'blog_posts', 'customers');
```

### الخطوة 2: إنشاء الجداول
إذا لم تظهر الجداول، شغل:
```sql
-- انسخ كل الكود من: database/fresh_dashboard_tables.sql
```

### الخطوة 3: إدراج البيانات التجريبية
```sql
-- انسخ كل الكود من: database/insert_sample_data.sql
```

## 🔄 بعد تشغيل الكود:

1. **أعد تحميل الصفحة** في المتصفح (F5)
2. **افتح Console** (F12) وتأكد من عدم وجود أخطاء
3. **اذهب للوحة التحكم** > الطلبات أو المراجعات

## ✨ النتيجة المتوقعة:
- ✅ 5 طلبات تظهر في صفحة الطلبات
- ✅ 7 مراجعات تظهر في صفحة المراجعات
- ✅ 8 عملاء في صفحة العملاء
- ✅ 5 مقالات في صفحة المقالات

## 🐛 إذا استمرت المشكلة:

### تحقق من الاتصال بـ Supabase:
```typescript
// في ملف .env تأكد من:
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### تحقق من RLS Policies:
```sql
-- تأكد أن الـ Policies موجودة
SELECT tablename, policyname 
FROM pg_policies 
WHERE tablename IN ('orders', 'reviews', 'customers', 'blog_posts');
```

### إذا كانت Policies غير موجودة:
```sql
-- شغل الكود من fresh_dashboard_tables.sql مرة أخرى
-- سيقوم بإنشاء الـ Policies تلقائياً
```

## 📝 ملاحظات:
- تأكد أنك تشغل الكود في **SQL Editor** في Supabase Dashboard
- **لا تنسخ** علامات التعليقات `--` أو النصوص التوضيحية
- شغل الملفات **بالترتيب**: الجداول أولاً ثم البيانات

**بعد هذه الخطوات، كل شيء سيعمل!** 🎉
