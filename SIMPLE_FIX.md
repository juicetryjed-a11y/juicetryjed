# 🔧 حل سريع لمشكلة الأعمدة المفقودة

## 💥 المشكلة:
```
Could not find the 'calories' column
```

## ✅ الحل الأسرع - خياران:

### الخيار 1️⃣: إضافة الأعمدة المفقودة (موصى به)

**شغل في Supabase SQL Editor:**
```sql
-- انسخ الكود من:
database/ADD_MISSING_COLUMNS.sql
```

**هذا سيضيف:**
- ✅ عمود `calories`
- ✅ عمود `ingredients`
- ✅ عمود `is_available`

**بعدها:**
- أعد بناء المشروع: `npm run build`
- ارفع على Netlify: `netlify deploy --prod --dir=dist`
- ✅ **كل شيء سيعمل!**

---

### الخيار 2️⃣: تعطيل RLS (الأسهل للتطوير)

**شغل في Supabase SQL Editor:**
```sql
-- انسخ الكود من:
database/DISABLE_RLS_ALL.sql
```

**ثم شغل:**
```sql
-- انسخ الكود من:
database/ADD_MISSING_COLUMNS.sql
```

**النتيجة:**
- ✅ RLS معطل
- ✅ الأعمدة موجودة
- ✅ لا توجد مشاكل

---

## 🎯 التوصية:

**شغل الملفين بالترتيب:**

1. **أولاً:** `ADD_MISSING_COLUMNS.sql`
2. **ثانياً:** `DISABLE_RLS_ALL.sql`

**ثم:**
```bash
npm run build
netlify deploy --prod --dir=dist
```

**كل شيء سيعمل بدون مشاكل!** 🎉
