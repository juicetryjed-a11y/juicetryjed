# دليل إدارة المستخدمين - User Management Guide

## نظرة عامة

تم إنشاء نظام متكامل لإدارة المستخدمين في لوحة تحكم Juicetry. النظام يوفر وظائف CRUD كاملة مع واجهة مستخدم حديثة وسهلة الاستخدام.

## المكونات الرئيسية

### 1. ملفات النظام
- `src/types/user.ts` - تعريفات الأنواع والواجهات
- `src/lib/userManagement.ts` - دوال API للتعامل مع قاعدة البيانات
- `src/components/dashboard/UsersManagement.tsx` - واجهة المستخدم الرئيسية
- `src/pages/admin/CompleteDashboard.tsx` - لوحة التحكم المحدثة

### 2. قاعدة البيانات
النظام يستخدم الجداول التالية:
- `users` - معلومات المستخدمين الأساسية
- `profiles` - معلومات الملف الشخصي الموسعة
- `auth.users` - مستخدمو المصادقة (Supabase Auth)

## الوظائف المتاحة

### 🔍 عرض المستخدمين
- عرض جميع المستخدمين مع معلوماتهم الكاملة
- بحث فوري بالاسم، البريد الإلكتروني، أو الهاتف
- فلترة حسب الدور (مدير/عميل)
- تحديث تلقائي للبيانات

### ➕ إنشاء مستخدم جديد
- إدخال البريد الإلكتروني وكلمة المرور
- تحديد الدور (مدير أو عميل)
- إضافة معلومات شخصية (الاسم، الهاتف، العنوان، المدينة)
- التحقق من صحة البيانات قبل الإرسال

### ✏️ تعديل المستخدم
- تحديث جميع معلومات المستخدم
- تغيير الدور بين مدير وعميل
- تعديل معلومات الملف الشخصي
- حفظ التغييرات فوراً

### 🗑️ حذف المستخدم
- حذف المستخدم من جميع الجداول
- حذف حساب المصادقة
- تأكيد قبل الحذف

### 🔄 تغيير الدور
- تبديل سريع بين مدير وعميل
- تحديث فوري في قاعدة البيانات
- رسائل تأكيد للنجاح

## واجهة المستخدم

### التصميم
- تصميم عصري متجاوب
- دعم كامل للغة العربية RTL
- ألوان متدرجة وأيقونات واضحة
- رسائل نجاح وأخطاء واضحة

### التفاعل
- نماذج منبثقة للإنشاء والتعديل
- أزرار تفاعلية مع تأثيرات حركية
- تحميل تلقائي للبيانات
- رسائل تأكيد لجميع العمليات

## الأمان

### الصلاحيات
- فقط المديرون يمكنهم الوصول لإدارة المستخدمين
- التحقق من صلاحيات المستخدم في كل عملية
- حماية دوال API

### البيانات
- التحقق من صحة البيانات المدخلة
- تشفير كلمات المرور
- حذف آمن للبيانات الحساسة

## الاستخدام

### 1. الوصول للوحة التحكم
```bash
# تشغيل الخادم
npm run dev

# الدخول للوحة التحكم
http://localhost:5173/admin/login
```

### 2. إدارة المستخدمين
1. تسجيل الدخول بحساب مدير
2. النقر على تبويب "المستخدمون"
3. استخدام الأزرار المتاحة للإدارة

### 3. العمليات المتاحة
- **بحث**: استخدام مربع البحث للعثور على مستخدمين
- **فلترة**: اختيار دور معين للعرض
- **إنشاء**: الضغط على "مستخدم جديد"
- **تعديل**: الضغط على أيقونة التعديل
- **حذف**: الضغط على أيقونة سلة المهملات
- **تغيير دور**: الضغط على أيقونة الدرع

## المتطلبات التقنية

### Frontend
- React 18 مع TypeScript
- Tailwind CSS للتصميم
- Lucide React للأيقونات
- React Hook Form للنماذج

### Backend
- Supabase لقاعدة البيانات
- PostgreSQL كنظام إدارة قواعد البيانات
- RLS Policies للأمان

## التخصيص

### إضافة حقول جديدة
1. تحديث `src/types/user.ts`
2. تعديل نماذج الإدخال في `UsersManagement.tsx`
3. تحديث دوال API في `userManagement.ts`

### تغيير التصميم
- تعديل الألوان في `UsersManagement.tsx`
- تغيير الأيقونات من Lucide React
- تعديل التخطيط في ملف CSS

## المشاكل والحلول

### مشكلة: لا يظهر المستخدمون
**الحل**: التحقق من اتصال قاعدة البيانات وصلاحيات RLS

### مشكلة: لا يمكن إنشاء مستخدم جديد
**الحل**: التأكد من صلاحيات المدير واتصال Supabase Auth

### مشكلة: أخطاء في الترجمة
**الحل**: التحقق من إعدادات RTL في Tailwind CSS

## التحديثات المستقبلية

### الميزات المقترحة
- استيراد/تصدير المستخدمين
- إرسال إشعارات للمستخدمين
- سجل التغييرات (Audit Log)
- إدارة الصلاحيات المتقدمة
- رسوم بيانية لإحصائيات المستخدمين

---

**ملاحظة**: هذا النظام جاهز للاستخدام في بيئة الإنتاج ويتوافق مع معايير الأمان الحديثة.
