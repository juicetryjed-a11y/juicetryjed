# 🗄️ دليل إعداد قاعدة البيانات الجديدة

## 🎯 **الهدف:**
إنشاء قاعدة بيانات جديدة كاملة وربطها بالموقع والداشبورد بدون بيانات تجريبية.

---

## 📋 **خطوات الإعداد:**

### **1. إنشاء مشروع Supabase جديد**

#### **أ. الذهاب إلى Supabase:**
1. اذهب إلى [https://supabase.com](https://supabase.com)
2. اضغط **"Start your project"**
3. سجل دخول أو أنشئ حساب جديد

#### **ب. إنشاء مشروع جديد:**
1. اضغط **"New Project"**
2. اختر **Organization** (أو أنشئ واحدة جديدة)
3. املأ بيانات المشروع:
   - **Name**: `juicetry-database`
   - **Database Password**: اختر كلمة مرور قوية (احفظها!)
   - **Region**: اختر أقرب منطقة لك
4. اضغط **"Create new project"**
5. انتظر حتى يتم إنشاء المشروع (2-3 دقائق)

---

### **2. الحصول على بيانات الاتصال**

#### **أ. الذهاب لإعدادات API:**
1. في لوحة تحكم المشروع، اذهب إلى **Settings** (الإعدادات)
2. اضغط على **API** من القائمة الجانبية

#### **ب. نسخ البيانات المطلوبة:**
```
Project URL: https://your-project-id.supabase.co
anon public key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

### **3. تحديث ملف .env**

#### **أ. فتح ملف .env:**
```bash
# في مجلد المشروع
code .env
```

#### **ب. تحديث القيم:**
```env
# استبدل هذه القيم بالقيم من مشروع Supabase الخاص بك
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

---

### **4. إنشاء قاعدة البيانات**

#### **أ. فتح SQL Editor:**
1. في لوحة تحكم Supabase، اذهب إلى **SQL Editor**
2. اضغط **"New query"**

#### **ب. تشغيل ملف إعداد قاعدة البيانات:**
1. انسخ محتوى ملف `database/fresh_database_setup.sql`
2. الصق المحتوى في SQL Editor
3. اضغط **"Run"** أو **Ctrl+Enter**
4. انتظر حتى يكتمل التنفيذ (30-60 ثانية)

#### **ج. التأكد من نجاح الإعداد:**
```sql
-- تشغيل هذا الاستعلام للتأكد
SELECT 'قاعدة البيانات جاهزة! ✅' as status,
       COUNT(*) as tables_count
FROM information_schema.tables 
WHERE table_schema = 'public';
```

---

### **5. إعداد Storage للصور**

#### **أ. إنشاء Bucket:**
1. اذهب إلى **Storage** في لوحة تحكم Supabase
2. اضغط **"Create bucket"**
3. اسم الـ bucket: `images`
4. اجعله **Public** ✅
5. اضغط **"Create bucket"**

#### **ب. إعداد السياسات:**
```sql
-- تشغيل في SQL Editor
-- السماح للجميع بقراءة الصور
CREATE POLICY "Allow public read access" ON storage.objects
FOR SELECT USING (bucket_id = 'images');

-- السماح للمديرين برفع الصور
CREATE POLICY "Allow admin upload" ON storage.objects
FOR INSERT WITH CHECK (
  bucket_id = 'images' AND
  EXISTS (
    SELECT 1 FROM profiles 
    WHERE profiles.id = auth.uid() 
    AND profiles.role = 'admin'
  )
);
```

---

### **6. إنشاء مستخدم إداري**

#### **أ. إنشاء المستخدم في Authentication:**
1. اذهب إلى **Authentication** > **Users**
2. اضغط **"Add user"**
3. املأ البيانات:
   - **Email**: `admin@juicetry.com`
   - **Password**: `admin123` (أو كلمة مرور أخرى)
   - **Email Confirm**: ✅
4. اضغط **"Create user"**

#### **ب. ربط المستخدم بجدول profiles:**
```sql
-- تشغيل في SQL Editor
-- استبدل USER_ID بالـ ID الفعلي للمستخدم من صفحة Authentication
UPDATE profiles 
SET id = 'USER_ID_FROM_AUTH_PANEL'
WHERE email = 'admin@juicetry.com';
```

---

### **7. اختبار الاتصال**

#### **أ. تشغيل المشروع:**
```bash
npm run dev
```

#### **ب. اختبار تسجيل الدخول:**
1. اذهب إلى `http://localhost:5173/admin/login`
2. سجل دخول بـ:
   - **Email**: `admin@juicetry.com`
   - **Password**: `admin123`
3. يجب أن تدخل للداشبورد بنجاح

#### **ج. اختبار الوظائف:**
1. **إضافة تصنيف جديد** ✅
2. **إضافة منتج جديد** ✅
3. **رفع صورة** ✅
4. **تغيير إعدادات الموقع** ✅

---

## 🔧 **استكشاف الأخطاء:**

### **خطأ: "Invalid API key"**
- تأكد من صحة `VITE_SUPABASE_ANON_KEY`
- تأكد من عدم وجود مسافات إضافية

### **خطأ: "Failed to fetch"**
- تأكد من صحة `VITE_SUPABASE_URL`
- تأكد من أن المشروع نشط في Supabase

### **خطأ: "Row Level Security"**
- تأكد من تشغيل ملف `fresh_database_setup.sql` بالكامل
- تأكد من إنشاء المستخدم الإداري

### **خطأ: "Storage bucket not found"**
- تأكد من إنشاء bucket باسم `images`
- تأكد من أنه public

---

## 📊 **هيكل قاعدة البيانات:**

### **الجداول الرئيسية:**
- `profiles` - المستخدمين والمديرين
- `categories` - تصنيفات المنتجات
- `products` - المنتجات
- `orders` + `order_items` - الطلبات
- `customer_reviews` - مراجعات العملاء
- `blog_posts` - المقالات
- `site_settings` - إعدادات الموقع
- `header_settings` - إعدادات الهيدر

### **المميزات:**
- ✅ **Row Level Security** مفعل
- ✅ **Triggers تلقائية** للتحديث
- ✅ **فهارس للأداء** السريع
- ✅ **Storage للصور** مُعد
- ✅ **سياسات أمان** محددة

---

## 🎉 **النتيجة النهائية:**

### ✅ **قاعدة بيانات كاملة:**
- **9 جداول رئيسية** مع العلاقات
- **بيانات افتراضية** للإعدادات
- **أمان متقدم** مع RLS

### ✅ **موقع متصل بالكامل:**
- **رفع المنتجات** يعمل
- **إضافة التصنيفات** يعمل
- **رفع الصور** يعمل
- **تغيير الإعدادات** يعمل

### ✅ **داشبورد متكامل:**
- **جميع الأقسام** متصلة بقاعدة البيانات
- **واجهات CRUD** كاملة
- **إحصائيات حية** من البيانات الحقيقية

---

**🚀 قاعدة البيانات جاهزة والموقع يعمل بكفاءة عالية!**

**⚡ جميع الوظائف متصلة بقاعدة البيانات الحقيقية!**

**💎 تجربة إدارة احترافية لموقع Juicetry! 🌟**
